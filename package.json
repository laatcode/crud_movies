{
  "name": "movies",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start_with_mysql:dev": "node --watch --env-file=.env src/startWithMySQL.js",
    "start_with_file:dev": "node --watch --env-file=.env src/startWithFile.js",
    "start_with_mysql": "node src/startWithMySQL.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "joi": "^18.0.2",
    "mysql2": "3.15.3"
  },
  "devDependencies": {
    "standard": "^17.1.2"
  },
  "eslintConfig": {
    "extends": "standard"
  }
}
